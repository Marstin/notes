<h1 align='center'><font color=red>js笔记</font></h1>

### ES6
* #### <font size=5 color=gree>class</font>
```javascript
    class {
      param:a; //error,不能这样定义属性
      constructor(param){ //在构造函数中定义属性
          this.param =param;
      };
      set param(value){

      }
    }
```
